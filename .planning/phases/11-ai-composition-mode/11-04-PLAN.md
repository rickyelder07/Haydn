---
phase: 11-ai-composition-mode
plan: "04"
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified: []
autonomous: false
requirements: [AI-01, AI-02, AI-03, AI-04, AI-06, AI-07, AI-08, AI-09, AI-10]

must_haves:
  truths:
    - "Template mode still generates MIDI as before (no regression)"
    - "AI Compose mode asks clarifying questions before generating"
    - "User can skip questions and generate immediately"
    - "Generated project loads into the piano roll with multiple tracks"
    - "Token usage and cost appear in the success banner after generation"
    - "Fallback to template is silent and still loads a project"
    - "Production build completes without errors"
  artifacts:
    - path: "src/lib/openai/aiComposeSchema.ts"
      provides: "Schema and converter — verified to compile"
    - path: "src/state/aiCompositionStore.ts"
      provides: "State machine — verified to orchestrate full flow"
    - path: "src/app/api/ai-compose/route.ts"
      provides: "API route — verified to return correct shape"
    - path: "src/components/GenerationInput.tsx"
      provides: "UI — verified to show mode toggle and AI flow"
  key_links:
    - from: "src/components/GenerationInput.tsx"
      to: "src/state/aiCompositionStore.ts"
      via: "useAiCompositionStore hook — verified to drive UI state"
      pattern: "useAiCompositionStore"
    - from: "src/state/aiCompositionStore.ts"
      to: "src/app/api/ai-compose/route.ts"
      via: "fetch('/api/ai-compose') — verified end-to-end with real GPT-4o call"
      pattern: "fetch.*api/ai-compose"
---

<objective>
Human verification of the complete AI Composition Mode. Run a production build, then manually exercise the full flow: template mode regression check, AI Compose with questions, skip flow, and review token/cost display.

Purpose: AI generation is subjective and interactive — only the user can confirm the questions are sensible, the generated music loads correctly, and the UX feels right.

Output: User approval confirming all AI-01 through AI-10 requirements are satisfied, or a list of issues for gap closure.
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build gate</name>
  <files></files>
  <action>
Run a production build to confirm no TypeScript errors or Next.js bundling issues were introduced by Phase 11 changes:

```bash
cd "/Users/rickyelder/Documents/Coding Apps/Haydn" && npm run build 2>&1 | tail -30
```

The build must succeed (exit 0, "Compiled successfully" or similar output). If it fails, fix the errors before proceeding to the human verification checkpoint.

Also confirm the dev server starts cleanly:
```bash
cd "/Users/rickyelder/Documents/Coding Apps/Haydn" && npx tsc --noEmit 2>&1 | head -20
```
  </action>
  <verify>
    <automated>cd "/Users/rickyelder/Documents/Coding Apps/Haydn" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build completes without errors. No TypeScript errors from npx tsc --noEmit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete AI Composition Mode end-to-end</name>
  <files></files>
  <action>Human verifies the complete AI Composition Mode flow including mode toggle, clarifying questions, skip flow, generation success, and token cost display. Template mode must not regress.</action>
  <verify>
    <automated>echo "Human verification required — see how-to-verify below"</automated>
  </verify>
  <done>User types "approved" confirming all 5 verification tests pass, or describes specific issues for gap closure.</done>
  <what-built>
Complete AI Composition Mode:
- Mode toggle (Template | AI Compose) at top of GenerationInput
- AI Compose flow: prompt → clarifying questions → answer inputs → generate or skip
- Validation retry loop (up to 3 attempts) with fallback to template
- Token usage and cost displayed after generation
- All existing template mode functionality unchanged
  </what-built>
  <how-to-verify>
Start the dev server: `npm run dev` then visit http://localhost:3000

**Test 1 — Template mode regression (no project loaded, empty state):**
1. Confirm "Template" tab is selected by default
2. Enter any prompt (e.g., "lofi beat in C minor") and click Generate
3. Confirm project loads into piano roll as before
4. Expected: no change from previous behavior

**Test 2 — AI Compose basic flow:**
1. Click "AI Compose" tab
2. Enter a brief prompt: "jazz tune"
3. Click "Compose" button
4. Confirm loading state appears ("Analyzing...")
5. Confirm 1-3 clarifying questions appear in a card
6. Type answers into the text fields
7. Click "Generate with Answers"
8. Confirm generating state appears ("Composing your music...")
9. Confirm project loads with multiple tracks (melody, chords, drums typically)
10. Confirm success banner shows token count and cost (~$0.01-$0.15 is expected)

**Test 3 — Skip questions:**
1. Click "AI Compose" tab (resets state)
2. Enter a prompt: "trap beat at 140 BPM in F minor"
3. Click "Compose"
4. When questions appear (or even before), click "Skip"
5. Confirm generation proceeds without answers
6. Confirm project loads successfully

**Test 4 — Check cost estimate display:**
1. In AI Compose mode, start composing
2. After questions appear and you answer them, before clicking Generate — confirm cost estimate appears ("Estimated cost: ~$0.0XX")

**Test 5 — AI Compose with detailed prompt (no questions expected):**
1. Enter a very detailed prompt: "lofi hip hop beat in C minor, 85 BPM, 16 bars, with piano chords, bass, and drums, melancholic mood"
2. Click "Compose"
3. GPT-4o may return 0 questions and auto-advance to generation
4. Confirm project loads without the Q&A step
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe specific issues (e.g., "questions didn't appear", "tracks missing", "token display wrong")</resume-signal>
</task>

</tasks>

<verification>
Human approval of all 5 verification tests confirms Phase 11 requirements AI-01 through AI-10 are satisfied.
</verification>

<success_criteria>
- Production build passes (npm run build exits 0)
- Template mode works identically to before (no regression)
- AI Compose mode shows mode toggle, questions, answer inputs, skip button
- AI-generated project loads into piano roll with multiple tracks
- Token usage and cost displayed in success banner
- Skip flow works (generate without answering questions)
- Fallback to template when AI fails (amber banner shown, project still loads)
- All requirements AI-01 through AI-10 confirmed by user
</success_criteria>

<output>
After completion, create `.planning/phases/11-ai-composition-mode/11-04-SUMMARY.md`
</output>
