---
phase: 01-foundation-a-midi-infrastructure
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - src/app/page.tsx
  - src/components/ErrorDisplay/ErrorDisplay.tsx
  - src/components/ErrorDisplay/index.ts
autonomous: false

must_haves:
  truths:
    - "Main page integrates all components into complete flow"
    - "User can upload MIDI file and see parsed data"
    - "User can upload MusicXML file and see parsed data"
    - "User can export project as MIDI"
    - "Errors display clearly to user"
    - "Empty state shows file upload prompt"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Main application page with all components"
      min_lines: 40
    - path: "src/components/ErrorDisplay/ErrorDisplay.tsx"
      provides: "Error message display component"
      min_lines: 20
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/FileUpload"
      via: "import"
      pattern: "import.*FileUpload"
    - from: "src/app/page.tsx"
      to: "src/components/TrackList"
      via: "import"
      pattern: "import.*TrackList"
    - from: "src/app/page.tsx"
      to: "src/components/MetadataDisplay"
      via: "import"
      pattern: "import.*MetadataDisplay"
    - from: "src/app/page.tsx"
      to: "src/components/ExportButton"
      via: "import"
      pattern: "import.*ExportButton"
---

<objective>
Integrate all components into the main page and verify the complete Phase 1 flow works end-to-end.

Purpose: This is the final integration that brings together all the pieces. The user should be able to upload a file, see the parsed data, and export - the complete Phase 1 experience.

Output: A working application where users can import MIDI/MusicXML, view parsed tracks and metadata, and export to standard MIDI format.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-a-midi-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-a-midi-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-a-midi-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-foundation-a-midi-infrastructure/01-02-SUMMARY.md
@.planning/phases/01-foundation-a-midi-infrastructure/01-03-SUMMARY.md
@.planning/phases/01-foundation-a-midi-infrastructure/01-04-SUMMARY.md
@.planning/phases/01-foundation-a-midi-infrastructure/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ErrorDisplay component</name>
  <files>
    src/components/ErrorDisplay/ErrorDisplay.tsx
    src/components/ErrorDisplay/index.ts
  </files>
  <action>
Create a component to display errors from the project store.

**src/components/ErrorDisplay/ErrorDisplay.tsx:**

```tsx
'use client';

import { useProjectStore } from '@/state/projectStore';

export function ErrorDisplay() {
  const { error, setError } = useProjectStore();

  if (!error) {
    return null;
  }

  return (
    <div className="w-full bg-red-50 border border-red-200 rounded-lg p-4">
      <div className="flex items-start gap-3">
        <svg
          className="w-5 h-5 text-red-500 flex-shrink-0 mt-0.5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            strokeLinecap="round"
            strokeLinejoin="round"
            strokeWidth={2}
            d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
          />
        </svg>
        <div className="flex-1 min-w-0">
          <h3 className="text-sm font-medium text-red-800">
            Error loading file
          </h3>
          <p className="text-sm text-red-600 mt-1">
            {error}
          </p>
        </div>
        <button
          onClick={() => setError(null)}
          className="flex-shrink-0 text-red-400 hover:text-red-600 transition-colors"
        >
          <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  );
}
```

**src/components/ErrorDisplay/index.ts:**
```typescript
export { ErrorDisplay } from './ErrorDisplay';
```
  </action>
  <verify>
Run `npx tsc --noEmit` to verify TypeScript compiles.
  </verify>
  <done>
- ErrorDisplay shows error message from store
- Includes dismiss button
- Hidden when no error
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate components in main page</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Update the main page to integrate all components into a cohesive layout.

**src/app/page.tsx:**

```tsx
'use client';

import { useProjectStore } from '@/state/projectStore';
import { FileUpload } from '@/components/FileUpload';
import { TrackList } from '@/components/TrackList';
import { MetadataDisplay } from '@/components/MetadataDisplay';
import { ExportButton } from '@/components/ExportButton';
import { ErrorDisplay } from '@/components/ErrorDisplay';

export default function Home() {
  const { project } = useProjectStore();

  return (
    <main className="min-h-screen bg-gray-50">
      {/* Header */}
      <header className="bg-white border-b border-gray-200">
        <div className="max-w-4xl mx-auto px-4 py-4 flex items-center justify-between">
          <h1 className="text-xl font-bold text-gray-900">
            Haydn
          </h1>
          {project && <ExportButton />}
        </div>
      </header>

      {/* Main Content */}
      <div className="max-w-4xl mx-auto px-4 py-8">
        {/* Error Display */}
        <div className="mb-6">
          <ErrorDisplay />
        </div>

        {/* File Upload - always visible */}
        <div className="mb-8">
          <FileUpload />
        </div>

        {/* Project Content - only when project loaded */}
        {project && (
          <div className="space-y-8">
            {/* Metadata Section */}
            <MetadataDisplay />

            {/* Track List */}
            <TrackList />
          </div>
        )}

        {/* Empty State */}
        {!project && (
          <div className="text-center py-12">
            <p className="text-gray-500">
              Upload a MIDI or MusicXML file to get started
            </p>
          </div>
        )}
      </div>

      {/* Footer */}
      <footer className="border-t border-gray-200 mt-auto">
        <div className="max-w-4xl mx-auto px-4 py-4 text-center text-sm text-gray-400">
          Haydn - Natural Language MIDI Editing
        </div>
      </footer>
    </main>
  );
}
```
  </action>
  <verify>
1. Run `npm run dev` and visit http://localhost:3000
2. Verify the app loads without errors
3. Verify the header shows "Haydn"
4. Verify the empty state shows upload prompt
5. Run `npm run build` to verify production build works
  </verify>
  <done>
- Main page integrates all components
- Header with title and export button
- File upload always visible
- Metadata and track list appear when project loaded
- Empty state when no project
- Footer with app name
- TypeScript compiles and app runs
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify complete Phase 1 flow</name>
  <what-built>
Complete Phase 1 implementation:
- File upload (drag-drop and click) for MIDI and MusicXML
- MIDI parser with validation
- MusicXML parser with conversion to internal format
- Track list showing name, instrument, note count, duration
- Collapsible metadata section
- MIDI export with download
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Open http://localhost:3000

**Test MIDI Import:**
3. Find a MIDI file on your computer (or download one from the internet)
4. Drag-and-drop it onto the upload area
5. Verify:
   - Tracks appear with names and instrument info
   - Note counts and durations display
   - Metadata section shows tempo, time signature
   - No errors

**Test MusicXML Import (if you have a .musicxml file):**
6. Click the upload area and select a MusicXML file
7. Verify it parses and displays similar info

**Test Export:**
8. Click "Export MIDI" button in header
9. Verify a .mid file downloads
10. (Optional) Open the exported file in a DAW to verify it works

**Test Error Handling:**
11. Try uploading a non-MIDI file (like a .txt)
12. Verify an error message appears

**Test Re-import Warning:**
13. With a project loaded, try uploading another file
14. Verify the confirmation dialog appears
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
After human verification:

1. All Phase 1 requirements met:
   - IO-01: MIDI upload works
   - IO-02: MusicXML upload works
   - IO-03: MIDI export works
   - IO-04: Track, tempo, note data preserved
   - IO-05: Exported MIDI compatible with DAWs (user verified)
   - TECH-01: Using @tonejs/midi library
   - TECH-03: Data in JSON format (HaydnProject)

2. App runs in dev and production modes
3. No TypeScript errors
4. All components integrate correctly
</verification>

<success_criteria>
- User can upload MIDI file and see parsed tracks/metadata
- User can upload MusicXML file and see parsed tracks/metadata
- User can export to MIDI and download the file
- Errors display clearly with dismiss option
- Empty state guides user to upload
- Re-import shows confirmation dialog
- App builds and runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-a-midi-infrastructure/01-06-SUMMARY.md`
</output>
