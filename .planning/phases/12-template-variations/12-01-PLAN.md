---
phase: 12-template-variations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/nl-generation/genreTemplates.ts
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-03, TMPL-06]

must_haves:
  truths:
    - "Each genre has 3-5 verse chord progression variations (not just 2)"
    - "Each genre has 3-5 chorus chord progression variations (not just 2)"
    - "Each genre has exactly 8 drum fill patterns in a drumFills array"
    - "Classical genre has drumFills: [] (empty, no fills for drumless genre)"
    - "All chord variations use genre-appropriate diatonic vocabulary"
    - "All drum fill tick offsets stay within [0, 1919] (single bar at PPQ=480)"
  artifacts:
    - path: "src/lib/nl-generation/genreTemplates.ts"
      provides: "Extended GenreTemplate interface + all 6 genres with expanded data"
      contains: "drumFills: DrumPattern[]"
      min_lines: 600
  key_links:
    - from: "src/lib/nl-generation/genreTemplates.ts"
      to: "src/lib/nl-generation/rhythmGenerator.ts"
      via: "drumFills field on GenreTemplate interface"
      pattern: "drumFills"
    - from: "src/lib/nl-generation/genreTemplates.ts"
      to: "src/lib/nl-generation/chordGenerator.ts"
      via: "chordProgressions expanded arrays"
      pattern: "chordProgressions"
---

<objective>
Expand genreTemplates.ts with 3-5 chord progressions per section type per genre and 8 drum fill patterns per genre, plus extend the GenreTemplate interface with drumFills.

Purpose: Provides the raw data that enables random variation in template generation. Without expanded progressions and fill arrays, the randomization logic in Plan 02 has nothing to select from.
Output: genreTemplates.ts with extended GenreTemplate interface and all 6 genres fully populated with new variation data.
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-template-variations/12-RESEARCH.md
@src/lib/nl-generation/genreTemplates.ts

<interfaces>
<!-- Current GenreTemplate interface (from genreTemplates.ts) — extend with drumFills -->

```typescript
export interface DrumPattern {
  kick: number[];           // Tick offsets for kick drum (MIDI 36)
  snare: number[];          // Tick offsets for snare drum (MIDI 38)
  hihat: number[];          // Tick offsets for closed hi-hat (MIDI 42)
  openHihat: number[];      // Tick offsets for open hi-hat (MIDI 46)
  velocityVariation: number; // 0-1, amount of random velocity variation
}

export interface GenreTemplate {
  name: string;
  chordProgressions: Record<SectionType, string[][]>;
  drumPatterns: Record<SectionType, DrumPattern>;
  // ADD: drumFills: DrumPattern[];
  defaultInstrumentation: { drums: number; bass: number; chords: number; melody: number; };
  melodyConfig: MelodyConfig;
  bassConfig: BassConfig;
}
```

<!-- Current chord progressions per genre — all have exactly 2 per verse/chorus.
     PPQ=480 → 1 bar = 4*480 = 1920 ticks → valid fill offsets: [0, 1919] -->

<!-- Roman numeral vocabulary per genre (from research):
     lofi: maj7/min7 extensions (ii7, V7, Imaj7, IVmaj7, vi7, iii7, bVII7)
     trap: minor modal triads (i, iv, VI, VII, III, V) — no maj7/7 extensions
     boom-bap: mix of triads and 7ths (ii7, V7, Imaj7, IVmaj7, i7, iv7, iii7, vi7)
     jazz: secondary dominants ok (ii7, V7, Imaj7, vi7, iii7, VI7, IVmaj7, iv7, bIIImaj7, bVImaj7)
     classical: plain triads only (I, IV, V, vi, ii)
     pop: plain triads only (I, V, vi, IV, ii) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GenreTemplate interface with drumFills</name>
  <files>src/lib/nl-generation/genreTemplates.ts</files>
  <action>
Add `drumFills: DrumPattern[]` field to the `GenreTemplate` interface, immediately after the `drumPatterns` field. Add a JSDoc comment: `// 8 single-bar fill patterns, selected randomly at section boundaries (empty array for genres with no drums)`.

This is a breaking change to the interface — all 6 genre objects must be updated in Task 2 or TypeScript will error.

Do NOT change any existing fields, types, or exports. The change is purely additive.
  </action>
  <verify>
    <automated>cd "/Users/rickyelder/Documents/Coding Apps/Haydn" && npx tsc --noEmit 2>&1 | grep "genreTemplates" | head -20</automated>
  </verify>
  <done>Interface has drumFills field; TypeScript shows property missing errors on existing genre objects (expected — resolved in Task 2)</done>
</task>

<task type="auto">
  <name>Task 2: Author expanded chord progressions and drum fills for all 6 genres</name>
  <files>src/lib/nl-generation/genreTemplates.ts</files>
  <action>
Update all 6 genre objects in GENRE_TEMPLATES to:
1. Add `drumFills: DrumPattern[]` array (8 fills for genres with drums, empty array `[]` for classical)
2. Expand `verse` progressions from 2 to 3-5 entries
3. Expand `chorus` progressions from 2 to 3-5 entries
4. Leave bridge/intro/outro progressions as-is (they have 1 and are used less)

Authoring rules (enforce strictly):
- All fill tick offsets MUST be in range [0, 1919] — a bar at PPQ=480 is 1920 ticks (offsets 0..1919)
- Use only roman numerals proven to work with Tonal.js: `maj7`, `7`, `m7`, `dim7`, `sus4`, standard triads, minor variants `i/iv/v`. Do NOT use `aug`, `#`-prefixed, or `b`-prefixed numerals EXCEPT `bVII7` and `bIIImaj7`/`bVImaj7` which are already in the codebase and known to work.
- Each genre's new progressions must stay within its established diatonic vocabulary

Add a leading comment block for each genre showing the variation count, e.g.:
```typescript
// lofi: 4 verse progressions, 4 chorus progressions, 8 drum fills
```

**Per-genre target content:**

**lofi** (4 verse, 4 chorus, 8 fills):
- verse add: `['vi7', 'IVmaj7', 'ii7', 'V7']`, `['Imaj7', 'iii7', 'vi7', 'IVmaj7']`
- chorus add: `['vi7', 'IVmaj7', 'Imaj7', 'V7']`, `['iii7', 'vi7', 'IVmaj7', 'V7']`
- fills: sparse quarter-note and 8th-note variations with open hihat accents; velocityVariation: 0.15

**trap** (4 verse, 4 chorus, 8 fills):
- verse add: `['i', 'VII', 'i', 'VI']`, `['i', 'III', 'VII', 'VI']`
- chorus add: `['i', 'VI', 'VII', 'III']`, `['VII', 'VI', 'i', 'i']`
- fills: dense 16th hi-hat rolls, snare flams, 808-style kick patterns; velocityVariation: 0.25-0.3

**boom-bap** (4 verse, 4 chorus, 8 fills):
- verse add: `['Imaj7', 'vi7', 'ii7', 'V7']`, `['i7', 'iv7', 'VII', 'III']`
- chorus add: `['ii7', 'V7', 'iii7', 'vi7']`, `['vi7', 'IVmaj7', 'V7', 'Imaj7']`
- fills: swung 8th patterns with snare variations; velocityVariation: 0.1-0.15

**jazz** (4 verse, 4 chorus, 8 fills):
- verse add: `['ii7', 'V7', 'iii7', 'VI7']`, `['Imaj7', 'IV7', 'iii7', 'vi7']`
- chorus add: `['ii7', 'V7', 'Imaj7', 'vi7']`, `['iii7', 'VI7', 'ii7', 'V7']`
- fills: ghost note snare patterns, ride cymbal variations with open hihat; velocityVariation: 0.25

**classical** (3 verse, 3 chorus, drumFills: []):
- verse add: `['I', 'ii', 'V', 'I']`
- chorus add: `['I', 'vi', 'ii', 'V']`
- drumFills: [] (empty — classical returns early from generateDrumTrack before fills would ever be accessed)

**pop** (4 verse, 4 chorus, 8 fills):
- verse add: `['vi', 'IV', 'I', 'V']`, `['I', 'ii', 'IV', 'V']`
- chorus add: `['vi', 'V', 'IV', 'I']`, `['IV', 'I', 'V', 'vi']`
- fills: standard rock/pop fills with snare rolls and kick accents; velocityVariation: 0.1-0.15
  </action>
  <verify>
    <automated>cd "/Users/rickyelder/Documents/Coding Apps/Haydn" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- `npx tsc --noEmit` exits 0 (no TypeScript errors)
- Each of lofi/trap/boom-bap/jazz/pop has `drumFills` with 8 entries
- Classical has `drumFills: []`
- lofi/trap/boom-bap/jazz/pop verse arrays have 4+ entries
- lofi/trap/boom-bap/jazz/pop chorus arrays have 4+ entries
- classical verse/chorus have 3 entries
- All fill tick offsets are integers in [0, 1919]
  </done>
</task>

</tasks>

<verification>
Run: `cd "/Users/rickyelder/Documents/Coding Apps/Haydn" && npx tsc --noEmit`
Expected: 0 errors

Spot-check data integrity:
- `grep -c "drumFills" src/lib/nl-generation/genreTemplates.ts` → should return 7 (1 interface + 6 genre entries)
- `grep "drumFills: \[\]" src/lib/nl-generation/genreTemplates.ts` → should show classical entry
</verification>

<success_criteria>
- GenreTemplate interface has drumFills field
- All 6 genre objects compile without TypeScript errors
- lofi, trap, boom-bap, jazz, pop each have 4+ verse and 4+ chorus progressions
- classical has 3+ verse and 3+ chorus progressions
- lofi, trap, boom-bap, jazz, pop each have 8 drum fills in drumFills array
- classical has drumFills: []
- tsc --noEmit passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-template-variations/12-01-SUMMARY.md`
</output>
