---
phase: 10-synthesis-enhancement
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified: []
autonomous: false
requirements:
  - AUDIO-07

must_haves:
  truths:
    - "User can import a MIDI file and hear real instrument samples (not PolySynth) during playback"
    - "Audio quality is noticeably better than v1.0 (subjective: 'good enough to compose with')"
    - "Percussion (channel 9) continues to function correctly — drum tracks still play"
    - "Loading spinner appears during sample fetch, play button activates when ready"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete CDN sample loading system works end-to-end from the user's perspective: real instrument sounds, working percussion, visible loading state, and subjectively better audio quality than v1.0.

Purpose: AUDIO-07 is a subjective requirement — only the user can confirm "good enough to compose with." This plan gates on that human confirmation before Phase 10 is marked complete.
Output: User confirmation that audio quality meets the bar.
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm production build passes</name>
  <files></files>
  <action>
    Run `npm run build` to confirm a clean production build with no TypeScript or bundler errors from all Phase 10 changes. This catches any import issues not caught by `tsc --noEmit` alone (e.g., missing module resolutions in Next.js).

    If build fails: fix the specific error reported (likely an import path or missing export) before proceeding to the checkpoint.

    If build succeeds: confirm the build is clean and the app is ready for testing.
  </action>
  <verify>
    <automated>cd "/Users/rickyelder/Documents/Coding Apps/Haydn" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build exits with code 0. No TypeScript errors, no module resolution errors, no bundler errors from the Phase 10 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies audio quality improvement</name>
  <action>
    Present verification instructions to the user. The human must confirm audio quality meets the "good enough to compose with" bar (AUDIO-07 is subjective and requires human judgment).

    Steps for the user to follow:
    1. Run `npm run dev` and open http://localhost:3000
    2. Import a MIDI file with multiple instrument types (piano, strings, and a percussion/drum track work best)
    3. Observe: the play button shows a spinner/loading state while samples fetch from CDN
    4. Wait for the spinner to disappear — play button should become active
    5. Press Play (or Space) and listen
    6. Open browser DevTools Network tab, filter by the gleitz domain — confirm multiple JS files fetch in parallel (not one-at-a-time waterfall)
    7. Confirm percussion/drums still sound correct (they use SynthInstrument, not CDN samples)
    8. Compare audio quality to v1.0 — does it sound like real instruments?

    Expected results: piano sounds like a piano, strings like strings, brass like brass — clearly better than triangle-wave oscillators. Drums still work. Spinner visible during load.
  </action>
  <verify>
    <automated>MISSING — audio quality is subjective, requires human approval</automated>
    <manual>User listens to playback and confirms instruments sound like real recordings, not PolySynth oscillators. Spinner appears during sample load. Percussion still works.</manual>
  </verify>
  <done>User types "approved" confirming: (1) audio quality is noticeably better, (2) spinner appears during CDN load, (3) percussion still works, (4) no unhandled console errors.</done>
</task>

</tasks>

<verification>
User confirms via checkpoint that:
1. Real instrument samples play (subjectively better than v1.0 PolySynth)
2. Loading spinner appears and clears correctly
3. Percussion channel still works
4. No console errors (CORS, missing buffers, unhandled rejections)
</verification>

<success_criteria>
- Production build exits clean (code 0)
- User approves audio quality as "noticeably better" (AUDIO-07 confirmed)
- Percussion tracks function correctly (AUDIO-06 preserved)
- CDN loading is visible to user via spinner state during sample fetch
</success_criteria>

<output>
After completion, create `.planning/phases/10-synthesis-enhancement/10-03-SUMMARY.md`
</output>
