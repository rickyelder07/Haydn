---
phase: 10-synthesis-enhancement
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified: []
autonomous: false
requirements:
  - AUDIO-07

must_haves:
  truths:
    - "User confirms audio quality is noticeably better than v1.0"
    - "Piano sounds percussive, not droning"
    - "Strings have audible bloom (slow attack) and vibrato on sustained notes"
    - "Brass sounds punchy with short tail"
    - "No harsh high-frequency artifacts on any instrument"
  artifacts: []
  key_links: []
---

<objective>
Subjective audio quality validation by the user. Load a MIDI project, open the debug panel, and confirm each instrument category sounds noticeably better than v1.0.

Purpose: AUDIO-07 is explicitly a subjective requirement — "sounds noticeably better" cannot be confirmed by automated tests. This plan captures the human-in-the-loop verification step.

Output: User approval (or list of tuning adjustments needed via debug panel).
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-synthesis-enhancement/10-CONTEXT.md
@.planning/phases/10-synthesis-enhancement/10-01-SUMMARY.md
@.planning/phases/10-synthesis-enhancement/10-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Audio quality subjective validation</name>
  <action>
    Human verifies that all instrument categories sound noticeably better than v1.0 baseline.
    Uses the SynthDebugPanel to tune ADSR and filter values in real-time if needed.
  </action>
  <what-built>
    Phase 10 complete implementation:
    - SynthInstrument: per-category ADSR envelopes, lowpass filter (-24dB/oct), string vibrato with delayed fade-in, velocity-to-attack mapping
    - PianoSampler: lowpass filter at 3800Hz
    - SynthDebugPanel: real-time ADSR + filter sliders per instrument category
    - Page.tsx: debug panel mounted behind ?debug=synth
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` from the project root
    2. Open http://localhost:3000 in Chrome or Edge
    3. Import any multi-instrument MIDI file (something with piano, strings, and brass ideally)
    4. Open the debug panel: add ?debug=synth to the URL (http://localhost:3000?debug=synth)
    5. Press Play and listen to each instrument category:
       - Piano: should sound percussive — note should fade out quickly, not drone
       - Strings: should bloom in (not instant attack) and have subtle vibrato on held notes
       - Brass: should be punchy — fast articulation with medium tail
    6. If any instrument needs tuning, use the debug panel sliders to adjust ADSR/filter values in real-time
    7. Continue until satisfied that audio quality is noticeably better than before
    8. Test with a simple MIDI that uses only piano — confirm it also sounds better (PianoSampler filter)
  </how-to-verify>
  <verify>
    <automated>MISSING — AUDIO-07 is a subjective requirement; no automated command can verify "sounds noticeably better"</automated>
    <manual>User listens to piano (percussive), strings (bloom + vibrato), brass (punchy) and confirms improvement over v1.0</manual>
  </verify>
  <done>User confirms audio quality meets "good enough to compose with" bar for all instrument categories</done>
  <resume-signal>Type "approved" when audio quality meets the "good enough to compose with" bar, or describe specific issues with instrument categories that need code-level fixes</resume-signal>
</task>

</tasks>

<verification>
User confirmation that audio quality is noticeably better than v1.0 baseline.
</verification>

<success_criteria>
- User confirms: piano sounds percussive (not sustained droning)
- User confirms: strings bloom gradually and have vibrato on held notes
- User confirms: brass sounds punchy
- User confirms: overall audio quality is "noticeably better than v1.0"
- AUDIO-07 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-synthesis-enhancement/10-03-SUMMARY.md`
</output>
