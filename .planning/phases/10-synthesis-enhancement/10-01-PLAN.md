---
phase: 10-synthesis-enhancement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio/instruments/gmInstrumentNames.ts
  - src/audio/instruments/soundfontLoader.ts
  - src/audio/instruments/SamplerInstrument.ts
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - AUDIO-04
  - AUDIO-05

must_haves:
  truths:
    - "A 128-entry array maps every GM program number to its gleitz CDN filename"
    - "loadSoundfontNotes(gmProgram) fetches the gleitz JS file, parses it to a note:dataURL map, and caches by program number"
    - "SamplerInstrument wraps Tone.Sampler, implements InstrumentInstance, and resolves load() when onload fires"
  artifacts:
    - path: "src/audio/instruments/gmInstrumentNames.ts"
      provides: "GM_SOUNDFONT_NAMES array and getSoundfontUrl() function"
      contains: "acoustic_grand_piano"
    - path: "src/audio/instruments/soundfontLoader.ts"
      provides: "loadSoundfontNotes() + clearSoundfontCache()"
      exports: ["loadSoundfontNotes", "clearSoundfontCache"]
    - path: "src/audio/instruments/SamplerInstrument.ts"
      provides: "SamplerInstrument class implementing InstrumentInstance"
      exports: ["SamplerInstrument"]
  key_links:
    - from: "src/audio/instruments/soundfontLoader.ts"
      to: "gleitz CDN"
      via: "fetch(getSoundfontUrl(gmProgram))"
      pattern: "gleitz\\.github\\.io/midi-js-soundfonts/FluidR3_GM"
    - from: "src/audio/instruments/SamplerInstrument.ts"
      to: "tone"
      via: "new Sampler({ urls, onload, onerror })"
      pattern: "new Sampler"
---

<objective>
Create the three new files that form the CDN sample loading foundation: the GM name map, the soundfont loader, and the SamplerInstrument class.

Purpose: Before wiring into InstrumentFactory, these files must exist and work independently. This plan creates all three without touching any existing files.
Output: gmInstrumentNames.ts (name lookup), soundfontLoader.ts (CDN fetch/parse/cache), SamplerInstrument.ts (Tone.Sampler wrapper).
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-synthesis-enhancement/10-RESEARCH.md
@src/audio/instruments/PianoSampler.ts
@src/audio/instruments/InstrumentFactory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GM name map and soundfont loader</name>
  <files>
    src/audio/instruments/gmInstrumentNames.ts
    src/audio/instruments/soundfontLoader.ts
  </files>
  <action>
    Create `src/audio/instruments/gmInstrumentNames.ts`:
    - Export a `const GM_SOUNDFONT_NAMES: readonly string[]` array with exactly 128 entries (index = GM program number).
    - Use the exact names from the RESEARCH.md Pattern 4 — these match the gleitz CDN filenames verbatim.
    - Export `function getSoundfontUrl(gmProgram: number): string` that returns `https://gleitz.github.io/midi-js-soundfonts/FluidR3_GM/${GM_SOUNDFONT_NAMES[gmProgram]}-mp3.js`. Throw `Error(`Invalid GM program: ${gmProgram}`)` if the name is undefined.

    Create `src/audio/instruments/soundfontLoader.ts`:
    - Export `async function loadSoundfontNotes(gmProgram: number): Promise<Record<string, string>>`.
    - Use a module-level `const fetchCache = new Map<number, Promise<Record<string, string>>>()` to deduplicate concurrent/repeated fetches for the same program.
    - Check the cache first; if a promise exists, return it directly (callers sharing the same instrument load in parallel and all await the same promise).
    - On cache miss: build the URL via `getSoundfontUrl(gmProgram)`, create a fetch promise that calls `res.text()` then `parseSoundfontJs(text)`, store the promise in the cache, and return it.
    - Implement `function parseSoundfontJs(text: string): Record<string, string>` (not exported) following the RESEARCH.md Pattern 2 algorithm exactly: `const begin = text.indexOf('MIDI.Soundfont.')`, throw if begin < 0, `const assignAt = text.indexOf('=', begin) + 2`, `const end = text.lastIndexOf(',')`, return `JSON.parse(text.slice(assignAt, end) + '}')`.
    - Export `function clearSoundfontCache(): void` that calls `fetchCache.clear()`.
    - If the fetch response is not ok, throw `Error(`Failed to fetch soundfont: ${url}`)`.
    - Do NOT catch errors here — let callers handle fallback.
  </action>
  <verify>
    <automated>cd "/Users/rickyelder/Documents/Coding Apps/Haydn" && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Confirm GM_SOUNDFONT_NAMES has exactly 128 entries: open gmInstrumentNames.ts and count, or add a quick console check.</manual>
  </verify>
  <done>TypeScript compiles without errors for the two new files. GM_SOUNDFONT_NAMES[0] === "acoustic_grand_piano", GM_SOUNDFONT_NAMES[127] === "gunshot". parseSoundfontJs logic matches RESEARCH.md spec exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Create SamplerInstrument class</name>
  <files>
    src/audio/instruments/SamplerInstrument.ts
  </files>
  <action>
    Create `src/audio/instruments/SamplerInstrument.ts`:
    - Import `{ Sampler }` from `'tone'`.
    - Import `type { InstrumentInstance }` from `'./PianoSampler'` (this is where the interface lives until Plan 02 moves it).
    - Export `class SamplerInstrument implements InstrumentInstance`.
    - Private fields: `private sampler: Sampler | null = null` and `private urls: Record<string, string>`.
    - Constructor accepts `urls: Record<string, string>` and stores it.
    - `async load(): Promise<void>` — wraps in a Promise, creates `this.sampler = new Sampler({ urls: this.urls, onload: () => resolve(), onerror: (err) => reject(err) }).toDestination()`. DO NOT pass `baseUrl` — pass all data URLs directly in `urls` to avoid Tone.js bug #899 where baseUrl was prepended to data: URLs.
    - `triggerAttackRelease(note: string, duration: number, time: number, velocity: number): void` — use `Math.max(duration, 0.05)` for safeDuration, then call `this.sampler?.triggerAttackRelease(note, safeDuration, time, velocity)`.
    - `releaseAll(): void` — call `this.sampler?.releaseAll()`.
    - `dispose(): void` — call `this.sampler?.dispose()` then set `this.sampler = null`.
  </action>
  <verify>
    <automated>cd "/Users/rickyelder/Documents/Coding Apps/Haydn" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles without errors. SamplerInstrument correctly implements all four InstrumentInstance methods. No baseUrl option present in the Sampler constructor call.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` from the project root — zero TypeScript errors across all three new files. Confirm:
1. gmInstrumentNames.ts exports GM_SOUNDFONT_NAMES (128 items) and getSoundfontUrl()
2. soundfontLoader.ts exports loadSoundfontNotes() and clearSoundfontCache(), uses module-level cache
3. SamplerInstrument.ts implements InstrumentInstance with no baseUrl in Sampler constructor
</verification>

<success_criteria>
- Three new files created with no TypeScript errors
- parseSoundfontJs uses the exact algorithm from RESEARCH.md (indexOf MIDI.Soundfont., lastIndexOf comma, JSON.parse with appended })
- SamplerInstrument.load() uses onload/onerror callbacks (not Tone.loaded()) per RESEARCH.md Pattern 3
- fetchCache deduplicates: calling loadSoundfontNotes(0) twice returns the same Promise (not two fetches)
</success_criteria>

<output>
After completion, create `.planning/phases/10-synthesis-enhancement/10-01-SUMMARY.md`
</output>
