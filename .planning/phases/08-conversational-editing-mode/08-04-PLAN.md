---
phase: 08-conversational-editing-mode
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/app/page.tsx
  - src/components/EditModeToggle.tsx
autonomous: true

must_haves:
  truths:
    - "User can switch between single-shot and conversation editing modes"
    - "Conversation panel is visible when conversation mode selected"
    - "Single-shot CommandInput is visible when single-shot mode selected"
  artifacts:
    - path: "src/components/EditModeToggle.tsx"
      provides: "Toggle component for switching editing modes"
      min_lines: 20
    - path: "src/app/page.tsx"
      provides: "Updated page with mode state and conditional rendering"
      contains: "useState.*editMode"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ConversationPanel"
      via: "conditional render when mode is 'conversation'"
      pattern: "ConversationPanel"
    - from: "src/app/page.tsx"
      to: "src/components/CommandInput"
      via: "conditional render when mode is 'single-shot'"
      pattern: "CommandInput"
---

<objective>
Integrate conversation mode into the main page with a toggle to switch between single-shot and conversation editing.

Purpose: Allow users to choose their preferred editing workflow - quick single-shot edits or multi-turn conversations for complex changes.

Output: Mode toggle component, updated page layout with conditional rendering of editing interfaces.
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/page.tsx
@src/components/CommandInput.tsx
@src/components/ConversationPanel/ConversationPanel.tsx (from 08-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EditModeToggle component</name>
  <files>src/components/EditModeToggle.tsx</files>
  <action>
Create a toggle component for switching between editing modes:

1. **Props:**
   - mode: 'single-shot' | 'conversation'
   - onChange: (mode: 'single-shot' | 'conversation') => void

2. **Design:**
   - Two-button toggle group (like segment control)
   - "Quick Edit" for single-shot (with tooltip: "One-off edits without context")
   - "Conversation" for conversation mode (with tooltip: "Multi-turn edits with memory")
   - Active state: bg-blue-600 text-white
   - Inactive state: bg-gray-100 text-gray-700 hover:bg-gray-200
   - Rounded corners, small text, compact design

3. **Layout:**
   - Inline flex, border, rounded-lg container
   - Each button fills half width equally
   - Smooth transition on hover/active states

4. **Icons (optional but nice):**
   - Lightning bolt SVG for Quick Edit (fast, single action)
   - Chat bubble SVG for Conversation (multi-turn)
   - Keep icons inline with text, small (w-4 h-4)

Use 'use client' directive. Style consistently with existing UI components.
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit src/components/EditModeToggle.tsx`</verify>
  <done>
- EditModeToggle renders two-button toggle
- Active/inactive states styled correctly
- onChange callback works correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate mode toggle and ConversationPanel into page</name>
  <files>src/app/page.tsx</files>
  <action>
Update the main page to support both editing modes:

1. **Add state:**
   ```typescript
   const [editMode, setEditMode] = useState<'single-shot' | 'conversation'>('single-shot');
   ```

2. **Add EditModeToggle:**
   - Position above the editing interface area
   - Inside the conditional rendering block (only show when project loaded AND track selected)
   - Same container/padding as CommandInput

3. **Conditional rendering of editing interface:**
   - When editMode === 'single-shot': render CommandInput (existing)
   - When editMode === 'conversation': render ConversationPanel

4. **Layout considerations for ConversationPanel:**
   - ConversationPanel needs fixed height to be scrollable
   - Add height constraint: h-96 (384px) or similar
   - Same max-w-4xl container as other sections
   - Add padding/margin consistent with existing layout

5. **Imports:**
   - Add: EditModeToggle, ConversationPanel
   - Keep existing: CommandInput

The key is maintaining the existing layout flow while accommodating the taller ConversationPanel when in conversation mode.
  </action>
  <verify>
1. `npm run build` succeeds (no Next.js build errors)
2. Page loads without console errors
3. Mode toggle is visible when project loaded and track selected
  </verify>
  <done>
- editMode state manages current editing mode
- EditModeToggle visible when project/track loaded
- single-shot mode shows CommandInput
- conversation mode shows ConversationPanel
- Layout properly accommodates both interfaces
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Mode toggle switches between editing interfaces
- ConversationPanel renders correctly when selected
- CommandInput still works in single-shot mode
- No layout issues or overflow problems
</verification>

<success_criteria>
- Users can switch between single-shot and conversation modes
- Each mode displays its respective editing interface
- Layout is clean and consistent with existing UI
- Default mode is single-shot (familiar for existing users)
- Toggle is only visible when editing is available (project+track loaded)
</success_criteria>

<output>
After completion, create `.planning/phases/08-conversational-editing-mode/08-04-SUMMARY.md`
</output>
