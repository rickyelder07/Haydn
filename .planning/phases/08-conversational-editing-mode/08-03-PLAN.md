---
phase: 08-conversational-editing-mode
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/ConversationPanel/ConversationPanel.tsx
  - src/components/ConversationPanel/MessageList.tsx
  - src/components/ConversationPanel/MessageItem.tsx
  - src/components/ConversationPanel/PromptInput.tsx
autonomous: true

must_haves:
  truths:
    - "User can see conversation history with user prompts and assistant responses"
    - "User can type and submit new prompts in the conversation"
    - "Total cost and token usage is displayed for the conversation session"
  artifacts:
    - path: "src/components/ConversationPanel/ConversationPanel.tsx"
      provides: "Main container with header, message list, and input"
      min_lines: 50
    - path: "src/components/ConversationPanel/MessageList.tsx"
      provides: "Scrollable list of conversation turns"
      min_lines: 30
    - path: "src/components/ConversationPanel/MessageItem.tsx"
      provides: "Individual message bubble for user or assistant"
      min_lines: 40
    - path: "src/components/ConversationPanel/PromptInput.tsx"
      provides: "Text input with submit button"
      min_lines: 30
  key_links:
    - from: "src/components/ConversationPanel/ConversationPanel.tsx"
      to: "src/state/conversationStore.ts"
      via: "useConversationStore hook"
      pattern: "useConversationStore"
    - from: "src/components/ConversationPanel/MessageList.tsx"
      to: "src/components/ConversationPanel/MessageItem.tsx"
      via: "renders MessageItem for each turn"
      pattern: "MessageItem"
---

<objective>
Create the chat UI component for multi-turn conversations.

Purpose: Provide a user interface for viewing conversation history, submitting new prompts, and tracking costs - separate from the single-shot CommandInput.

Output: ConversationPanel component with message list, individual message rendering, and prompt input.
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-conversational-editing-mode/08-RESEARCH.md
@src/components/CommandInput.tsx
@src/state/conversationStore.ts (from 08-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MessageItem component</name>
  <files>src/components/ConversationPanel/MessageItem.tsx</files>
  <action>
Create component to render individual conversation turns:

1. **Props:**
   - turn: ConversationTurn
   - isLatest: boolean (for potential styling differences)

2. **User message bubble (right-aligned):**
   - Blue background (bg-blue-500 text-white)
   - Shows turn.userPrompt
   - Timestamp in small text (formatted with toLocaleTimeString)
   - max-w-[80%] for message width

3. **Assistant response bubble (left-aligned):**
   - Gray background (bg-gray-100)
   - Success/failure indicator: checkmark if executionResult.success, X if not
   - Shows turn.assistantResponse.summary
   - If warnings exist, show in orange text
   - If execution failed, show errors in red text
   - Token usage and cost in small gray text: "{totalTokens} tokens (~${cost.toFixed(4)})"

4. **Layout:**
   - Wrap in div with space-y-2 for vertical spacing
   - User message first, assistant response below
   - Use flexbox justify-end for user, justify-start for assistant

Follow the chat UI patterns from research code examples. Use Tailwind classes consistent with existing components (CommandInput.tsx).
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit src/components/ConversationPanel/MessageItem.tsx`</verify>
  <done>
- MessageItem renders user prompt and assistant response
- Shows success/failure status, summary, warnings, errors
- Displays token usage and cost
- Proper alignment and styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MessageList and PromptInput components</name>
  <files>
src/components/ConversationPanel/MessageList.tsx
src/components/ConversationPanel/PromptInput.tsx
  </files>
  <action>
**MessageList.tsx:**

1. **Props:**
   - turns: ConversationTurn[]
   - isLoading: boolean

2. **Features:**
   - Scrollable container (overflow-y-auto, flex-1)
   - Maps over turns, renders MessageItem for each
   - Pass isLatest={index === turns.length - 1} to MessageItem
   - Auto-scroll to bottom on new messages (useEffect with ref.scrollIntoView)
   - Loading indicator at bottom when isLoading: animated dots (same pattern as CommandInput)
   - Empty state message when no turns

**PromptInput.tsx:**

1. **Props:**
   - onSubmit: (prompt: string) => void
   - disabled: boolean
   - isLoading: boolean

2. **Features:**
   - Text input with 500 char max length
   - Submit button ("Send")
   - Disabled state when isLoading or disabled prop
   - Enter to submit (not Shift+Enter needed for single line)
   - Clear input after successful submit (call onSubmit, clear local state)
   - Character count display: "{length}/500 characters"

Use patterns from CommandInput.tsx for consistency. Both components should be 'use client'.
  </action>
  <verify>
1. TypeScript compiles: `npx tsc --noEmit src/components/ConversationPanel/MessageList.tsx`
2. TypeScript compiles: `npx tsc --noEmit src/components/ConversationPanel/PromptInput.tsx`
  </verify>
  <done>
- MessageList renders turns with auto-scroll and loading indicator
- PromptInput handles text input with submit and character count
- Both use consistent Tailwind styling
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ConversationPanel container</name>
  <files>src/components/ConversationPanel/ConversationPanel.tsx</files>
  <action>
Create the main container component:

1. **Header section:**
   - Title: session title or "No Active Conversation"
   - Cost display: "Cost: ${getTotalCost().toFixed(4)}"
   - "New" button: calls startNewSession (confirm if current session has turns)
   - "Clear" button: calls clearSession (disabled if no session)

2. **Body section:**
   - If no activeSession: show empty state with "Start Conversation" button
   - If activeSession: render MessageList with session.turns and isLoading
   - Takes flex-1 to fill available height

3. **Error banner (if error):**
   - Red background, shows error message
   - Between body and input

4. **Input section:**
   - Render PromptInput
   - disabled={!activeSession}
   - onSubmit calls handleSubmit function

5. **handleSubmit function:**
   - Build MIDI context using buildMIDIContext from @/lib/nl-edit/contextBuilder
   - Build conversation history using buildMessagesFromTurns from messageBuilder
   - Call /api/nl-conversation
   - Execute edits using executeEditOperations
   - Create ConversationTurn with result
   - Call addTurn to update store
   - Handle errors with setError

6. **Layout:**
   - Flex column, full height
   - Border with rounded corners
   - White background (matches existing UI)

Import from conversationStore: useConversationStore
Import from nl-edit: buildMIDIContext, executeEditOperations
Import from messageBuilder: buildMessagesFromTurns
Import types from nl-conversation/types

Create index.ts barrel export: `export { ConversationPanel } from './ConversationPanel';`
  </action>
  <verify>
1. TypeScript compiles: `npx tsc --noEmit src/components/ConversationPanel/ConversationPanel.tsx`
2. Index exports component: `grep -c "export.*ConversationPanel" src/components/ConversationPanel/index.ts` returns 1
  </verify>
  <done>
- ConversationPanel integrates MessageList, PromptInput, header with controls
- handleSubmit orchestrates API call, edit execution, and store update
- Error handling displays in banner
- Empty state guides user to start conversation
- Index file provides barrel export
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all component files
- ConversationPanel properly imports and uses child components
- Store integration works (useConversationStore)
- API integration complete (fetch to /api/nl-conversation)
- Edit execution integrated (executeEditOperations)
</verification>

<success_criteria>
- Users can see conversation history with proper message rendering
- Users can type prompts and submit them
- Loading state shows animated indicator
- Errors display in banner
- Token usage and costs displayed per message and total
- New/Clear buttons manage conversation sessions
</success_criteria>

<output>
After completion, create `.planning/phases/08-conversational-editing-mode/08-03-SUMMARY.md`
</output>
