---
phase: 09-professional-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/InlineEdit/InlineEdit.tsx
  - src/components/InlineEdit/index.ts
  - src/components/TrackList/TrackItem.tsx
  - src/components/MetadataDisplay/MetadataDisplay.tsx
  - src/state/projectStore.ts
autonomous: true

must_haves:
  truths:
    - "User can edit track names inline by clicking on them"
    - "User can edit project name inline by clicking on it"
    - "Enter confirms edit, Escape cancels edit"
    - "Edit mode has distinct visual styling"
  artifacts:
    - path: "src/components/InlineEdit/InlineEdit.tsx"
      provides: "Reusable inline editing component"
      min_lines: 50
    - path: "src/state/projectStore.ts"
      provides: "updateProjectName action"
      exports: ["useProjectStore"]
  key_links:
    - from: "src/components/TrackList/TrackItem.tsx"
      to: "src/state/projectStore.ts"
      via: "updateTrackName action"
      pattern: "updateTrackName"
    - from: "src/components/MetadataDisplay/MetadataDisplay.tsx"
      to: "src/state/projectStore.ts"
      via: "updateProjectName action"
      pattern: "updateProjectName"
---

<objective>
Create inline editing capability for track names and project name.

Purpose: Allow users to rename tracks and projects without modal dialogs, using standard DAW inline editing patterns.
Output: InlineEdit component and integration in TrackItem and MetadataDisplay.
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/TrackList/TrackItem.tsx
@src/components/MetadataDisplay/MetadataDisplay.tsx
@src/state/projectStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InlineEdit reusable component</name>
  <files>src/components/InlineEdit/InlineEdit.tsx, src/components/InlineEdit/index.ts</files>
  <action>
Create a reusable inline editing component:

1. Create `src/components/InlineEdit/InlineEdit.tsx`:
   ```typescript
   interface InlineEditProps {
     value: string;
     onSave: (newValue: string) => void;
     placeholder?: string;
     className?: string;
     inputClassName?: string;
     maxLength?: number;
   }
   ```

2. Component behavior (Claude's discretion: double-click to edit):
   - Default state: renders value as text (span or div)
   - Double-click on text enters edit mode
   - Edit mode: renders input field with current value
   - Auto-focus and select all text on edit mode enter
   - Enter key: save (call onSave with trimmed value) and exit edit mode
   - Escape key: cancel (revert to original value) and exit edit mode
   - Blur: save (same as Enter)
   - Prevent saving empty strings (revert to original)

3. Visual styling:
   - Normal state: inherit text styling from parent
   - Edit mode: subtle background highlight (bg-white/10), border (border-cyan-500/50), rounded
   - Cursor: text when hovering normal state
   - Smooth transition between states

4. Create `src/components/InlineEdit/index.ts` barrel export

Implementation details:
- Use useState for isEditing and localValue
- useRef for input element (for focus management)
- useEffect to focus input when isEditing becomes true
  </action>
  <verify>
Run: `npx tsc --noEmit src/components/InlineEdit/*.tsx` - no type errors
Check exports: `grep -E "export" src/components/InlineEdit/index.ts`
  </verify>
  <done>
InlineEdit component handles double-click to edit, Enter/Escape/blur behavior, visual styling for edit mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add updateProjectName action and integrate in MetadataDisplay</name>
  <files>src/state/projectStore.ts, src/components/MetadataDisplay/MetadataDisplay.tsx</files>
  <action>
1. Update `src/state/projectStore.ts`:
   - Add `updateProjectName: (name: string) => void` to interface
   - Implement action:
     ```typescript
     updateProjectName: (name) => set((state) => {
       if (!state.project) return state;
       return {
         project: {
           ...state.project,
           originalFileName: name
         }
       };
     })
     ```

2. Update `src/components/MetadataDisplay/MetadataDisplay.tsx`:
   - Import InlineEdit component
   - Replace the static project name display with InlineEdit
   - Current code shows: `{project.originalFileName || 'Untitled Project'}`
   - Replace with:
     ```tsx
     <InlineEdit
       value={project.originalFileName || 'Untitled Project'}
       onSave={(name) => updateProjectName(name)}
       className="font-medium text-primary"
     />
     ```
   - Add useProjectStore destructuring for updateProjectName
  </action>
  <verify>
Run: `npx tsc --noEmit src/state/projectStore.ts src/components/MetadataDisplay/MetadataDisplay.tsx`
Check updateProjectName exists: `grep "updateProjectName" src/state/projectStore.ts`
  </verify>
  <done>
Project name in MetadataDisplay is editable via double-click, changes persist in project state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate InlineEdit in TrackItem for track names</name>
  <files>src/components/TrackList/TrackItem.tsx</files>
  <action>
Update TrackItem to use InlineEdit for track names:

1. Import InlineEdit component
2. Import updateTrackName from projectStore (already exists)
3. Replace the static track name h3 element with InlineEdit:
   - Current: `<h3 className="font-medium text-primary truncate">{track.name}</h3>`
   - Replace with:
     ```tsx
     <InlineEdit
       value={track.name}
       onSave={(name) => updateTrackName(trackIndex, name)}
       className="font-medium text-primary"
       maxLength={50}
     />
     ```

4. Stop click propagation on the InlineEdit wrapper to prevent track selection while editing:
   - Wrap InlineEdit in a div with onClick={(e) => e.stopPropagation()}
   - Or handle this inside InlineEdit when in edit mode

5. Ensure the "Editing" badge still shows when track is selected
  </action>
  <verify>
Run: `npx tsc --noEmit src/components/TrackList/TrackItem.tsx`
Run: `npm run build` - full build passes
  </verify>
  <done>
Track names in TrackItem are editable via double-click, changes persist in project state, track selection not triggered during edit.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npm run build`
2. Double-click project name in MetadataDisplay enters edit mode
3. Double-click track name in TrackItem enters edit mode
4. Enter key saves changes
5. Escape key cancels changes
6. Clicking outside (blur) saves changes
7. Empty names revert to original
</verification>

<success_criteria>
- User can double-click project name to edit inline
- User can double-click track name to edit inline
- Enter confirms edit, Escape cancels edit
- Edit mode shows distinct visual styling (highlight/border)
- Changes persist in project state
- Track selection not triggered when editing track name
</success_criteria>

<output>
After completion, create `.planning/phases/09-professional-layout/09-02-SUMMARY.md`
</output>
