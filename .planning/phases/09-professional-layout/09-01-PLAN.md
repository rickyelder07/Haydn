---
phase: 09-professional-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar/Sidebar.tsx
  - src/components/Sidebar/index.ts
  - src/components/Sidebar/ResizeHandle.tsx
  - src/state/uiStore.ts
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can drag sidebar right edge to resize"
    - "User can collapse/expand sidebar with toggle button"
    - "Sidebar width persists across browser sessions"
    - "Sidebar respects min/max width constraints"
  artifacts:
    - path: "src/components/Sidebar/Sidebar.tsx"
      provides: "Resizable sidebar container with collapse toggle"
      min_lines: 80
    - path: "src/components/Sidebar/ResizeHandle.tsx"
      provides: "Draggable resize handle component"
      min_lines: 40
    - path: "src/state/uiStore.ts"
      provides: "UI state with localStorage persistence"
      exports: ["useUIStore"]
  key_links:
    - from: "src/components/Sidebar/Sidebar.tsx"
      to: "src/state/uiStore.ts"
      via: "useUIStore hook"
      pattern: "useUIStore"
    - from: "src/state/uiStore.ts"
      to: "localStorage"
      via: "persist middleware"
      pattern: "localStorage\\.getItem|localStorage\\.setItem|persist"
    - from: "src/app/page.tsx"
      to: "src/components/Sidebar/Sidebar.tsx"
      via: "children prop (TrackList, MetadataDisplay moved into Sidebar)"
      pattern: "<Sidebar>.*<TrackList|<MetadataDisplay"
---

<objective>
Create a resizable sidebar component with collapse/expand functionality and width persistence.

Purpose: Enable DAW-style layout with user-controlled sidebar width that persists across sessions.
Output: Sidebar component with resize handle, collapse toggle, and persisted state.
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/state/projectStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI state store with localStorage persistence</name>
  <files>src/state/uiStore.ts</files>
  <action>
Create a Zustand store for UI state with localStorage persistence:

1. Create `src/state/uiStore.ts`:
   - State: `sidebarWidth: number` (default 280), `sidebarCollapsed: boolean` (default false)
   - Constants: `SIDEBAR_MIN_WIDTH = 200`, `SIDEBAR_MAX_WIDTH = 480`
   - Actions: `setSidebarWidth(width: number)`, `toggleSidebar()`, `setSidebarCollapsed(collapsed: boolean)`
   - Use Zustand persist middleware with localStorage key 'haydn-ui-state'

2. Width setter should clamp between min/max:
   ```typescript
   setSidebarWidth: (width) => set({
     sidebarWidth: Math.max(SIDEBAR_MIN_WIDTH, Math.min(SIDEBAR_MAX_WIDTH, width))
   })
   ```

3. Export constants for use in components: `export { SIDEBAR_MIN_WIDTH, SIDEBAR_MAX_WIDTH }`

Follow existing store patterns from projectStore.ts (create from zustand, typed state interface).
  </action>
  <verify>
Run: `npx tsc --noEmit src/state/uiStore.ts` - no type errors
Verify exports with: `grep -E "export.*(useUIStore|SIDEBAR)" src/state/uiStore.ts`
  </verify>
  <done>
Store exists with sidebarWidth, sidebarCollapsed state, persist middleware configured, min/max constants exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ResizeHandle and Sidebar components</name>
  <files>src/components/Sidebar/Sidebar.tsx, src/components/Sidebar/ResizeHandle.tsx, src/components/Sidebar/index.ts</files>
  <action>
Create the Sidebar component folder with resize functionality:

1. Create `src/components/Sidebar/ResizeHandle.tsx`:
   - Vertical bar on right edge of sidebar
   - Cursor: `ew-resize` (or `col-resize`)
   - Visual: 4px wide, full height, transparent until hover (then subtle gray)
   - onMouseDown starts drag, window mousemove updates width, window mouseup ends drag
   - Pass `onResize(deltaX: number)` or `onResizeEnd(newWidth: number)` callback to parent

2. Create `src/components/Sidebar/Sidebar.tsx`:
   - Props: `children: React.ReactNode`
   - Use `useUIStore` for width/collapsed state
   - Render as flex column with fixed width from store
   - When collapsed: width = 0 or small icon-only width (48px)
   - Include collapse toggle button (chevron icon) in header
   - ResizeHandle positioned on right edge

3. Create `src/components/Sidebar/index.ts` barrel export

Layout structure:
```tsx
<aside style={{ width: collapsed ? 48 : sidebarWidth }} className="...">
  <div className="sidebar-header">
    <button onClick={toggleSidebar}>{chevronIcon}</button>
  </div>
  <div className="sidebar-content">
    {children}
  </div>
  {!collapsed && <ResizeHandle onResize={handleResize} />}
</aside>
```

Visual styling: Match existing glass-panel aesthetic with border-white/10.
  </action>
  <verify>
Run: `npx tsc --noEmit src/components/Sidebar/*.tsx` - no type errors
Check file structure: `ls src/components/Sidebar/`
  </verify>
  <done>
Sidebar component renders with collapse toggle, ResizeHandle enables drag-to-resize, width comes from store.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate Sidebar into page layout</name>
  <files>src/app/page.tsx</files>
  <action>
Restructure page.tsx to use the new Sidebar component:

1. Import Sidebar component
2. Wrap the main content area in a flex layout with sidebar:
   ```tsx
   <div className="flex h-screen">
     <Sidebar>
       {/* Move TrackList and MetadataDisplay here */}
       {project && <MetadataDisplay />}
       {project && <TrackList />}
     </Sidebar>
     <main className="flex-1 overflow-auto">
       {/* Keep header, main content, footer */}
     </main>
   </div>
   ```

3. Remove the current max-w-4xl sections for MetadataDisplay and TrackList (they go in sidebar)
4. Keep piano roll and transport in main content area with max-w-7xl
5. Adjust header to span full width above the flex layout

Ensure smooth transition with CSS: `transition: width 200ms ease`
  </action>
  <verify>
Run: `npm run build` - no build errors
Run: `npm run dev` and manually verify sidebar renders, resizes, and persists
  </verify>
  <done>
Page layout uses Sidebar component, content reorganized, resize and collapse work, width persists after refresh.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npm run build`
2. Sidebar resizes smoothly with mouse drag
3. Sidebar collapse button toggles between expanded/collapsed states
4. Refresh page and verify sidebar width is preserved
5. Test min/max constraints (try to drag beyond limits)
</verification>

<success_criteria>
- User can drag sidebar right edge and width changes fluidly
- User can click collapse button to toggle sidebar
- After page refresh, sidebar width is restored from localStorage
- Sidebar cannot be resized below 200px or above 480px
- TrackList and MetadataDisplay render inside sidebar when project loaded
</success_criteria>

<output>
After completion, create `.planning/phases/09-professional-layout/09-01-SUMMARY.md`
</output>
