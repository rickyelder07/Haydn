---
phase: 03-piano-roll-editor
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - src/app/page.tsx
  - src/components/TrackList/TrackList.tsx
  - src/hooks/useKeyboardShortcuts.ts
autonomous: true

must_haves:
  truths:
    - "Piano roll displays all tracks with visual distinction via track selection"
    - "User can select a track from the track list to edit in the piano roll"
    - "Piano roll integrates into the main page layout seamlessly"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Updated main page with PianoRollEditor integrated between transport and tracks"
      contains: "PianoRollEditor"
    - path: "src/components/TrackList/TrackList.tsx"
      provides: "Updated track list with click-to-select for piano roll editing"
      contains: "selectTrack"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/PianoRoll/index.ts"
      via: "Imports and renders PianoRollEditor"
      pattern: "PianoRollEditor"
    - from: "src/components/TrackList/TrackList.tsx"
      to: "src/state/editStore.ts"
      via: "Calls selectTrack on track card click"
      pattern: "selectTrack"
    - from: "src/app/page.tsx"
      to: "src/state/editStore.ts"
      via: "Auto-selects first track when project loads"
      pattern: "selectTrack"
---

<objective>
Integrate the PianoRollEditor into the main application page. Add track selection to TrackList so users can choose which track to edit. Wire up keyboard shortcuts for the piano roll.

Purpose: The piano roll needs to be accessible from the main page with a clear workflow: load project -> select track -> edit in piano roll. This completes the feature integration before human verification.
Output: Updated `page.tsx` with piano roll, updated `TrackList.tsx` with track selection, updated keyboard shortcuts.
</objective>

<execution_context>
@/Users/rickyelder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rickyelder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/components/TrackList/TrackList.tsx
@src/hooks/useKeyboardShortcuts.ts
@src/state/editStore.ts
@src/state/projectStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add track selection to TrackList</name>
  <files>src/components/TrackList/TrackList.tsx</files>
  <action>
Read the current TrackList.tsx first, then update it:

1. Import `useEditStore` from `@/state/editStore`
2. Get `selectedTrackIndex` and `selectTrack` from the edit store
3. Add click handler to each track card: `onClick={() => selectTrack(trackIndex)}`
   - Only count tracks with notes (matching the existing filter pattern from Phase 2)
   - Map the visible track index to the actual project track index
4. Highlight the selected track card:
   - Selected: `ring-2 ring-blue-500 bg-blue-50` border
   - Unselected: existing styling
5. Add a subtle "Editing" badge or indicator on the selected track card
6. Add cursor-pointer to track cards to indicate clickability

Keep all existing track card content (name, instrument, note count, duration). Just add the selection behavior and visual feedback.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit src/components/TrackList/TrackList.tsx`.
Check imports: `grep "editStore\|selectTrack" src/components/TrackList/TrackList.tsx`.
  </verify>
  <done>Track cards are clickable and highlight when selected. Selected track index syncs with editStore. Visual ring indicator shows which track is being edited.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate PianoRollEditor into main page</name>
  <files>
    src/app/page.tsx
    src/hooks/useKeyboardShortcuts.ts
  </files>
  <action>
**page.tsx:**
Read current page.tsx first, then update:

1. Import `PianoRollEditor` from `@/components/PianoRoll`
2. Import `useEditStore` from `@/state/editStore`
3. Get `selectedTrackIndex` and `selectTrack` from edit store

4. Add auto-select first track when project loads:
   - In the existing useEffect that calls loadProject, also call `selectTrack(0)` after loading
   - But only select the first track that has notes (filter empty tracks, same as TrackList)

5. Add PianoRollEditor between TransportControls and the project content sections:
```tsx
{project && selectedTrackIndex !== null && (
  <div className="mb-6">
    <PianoRollEditor />
  </div>
)}
```

6. Update the page layout to use full width for the piano roll section:
   - Change the outer container from `max-w-4xl` to `max-w-7xl` (or use a responsive approach)
   - The piano roll needs more horizontal space than the metadata/track list
   - Alternative: keep `max-w-4xl` for header/metadata/tracks but use full-width for piano roll

Recommended layout approach:
- Header: max-w-4xl (as before)
- TransportControls: max-w-7xl
- PianoRollEditor: max-w-7xl (wider for canvas)
- Metadata + TrackList: max-w-4xl (as before)

**useKeyboardShortcuts.ts:**
Read current file, then add:
- Delete/Backspace key: delete selected note(s) from editStore
  - Get selectedNoteIds from editStore
  - Parse note index from the selected ID
  - Call editStore.deleteNote(noteIndex)
- Only activate when a project is loaded (existing guard pattern)

Be careful not to conflict with existing shortcuts (Space for play/pause, etc.).
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit src/app/page.tsx src/hooks/useKeyboardShortcuts.ts`.
Check PianoRoll import: `grep "PianoRollEditor" src/app/page.tsx`.
Verify build: `npm run build` succeeds.
  </verify>
  <done>PianoRollEditor renders on the main page when a project is loaded. First track with notes auto-selected on project load. TrackList click changes which track is shown in piano roll. Delete key removes selected notes. Full page layout accommodates the wider piano roll.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. PianoRollEditor visible on page when project is loaded
3. Clicking a track in TrackList updates the piano roll to show that track's notes
4. First track auto-selected on project load
5. Delete key removes selected note
6. Page layout properly accommodates the wider piano roll
</verification>

<success_criteria>
- Piano roll appears after loading a MIDI file
- Track selection highlights in TrackList and shows corresponding notes in piano roll
- Auto-select first track on load (no blank piano roll)
- Delete key shortcut works for removing selected notes
- Page layout is clean with piano roll taking appropriate width
- All existing features (upload, playback, export) still work
</success_criteria>

<output>
After completion, create `.planning/phases/03-piano-roll-editor/03-05-SUMMARY.md`
</output>
